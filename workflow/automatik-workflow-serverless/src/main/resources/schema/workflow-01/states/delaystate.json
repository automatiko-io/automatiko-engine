{
  "type": "object",
  "javaType": "io.automatik.engine.workflow.serverless.api.states.DelayState",
  "javaInterfaces": [
    "io.automatik.engine.workflow.serverless.api.interfaces.State"
  ],
  "description": "This state is used to wait for events from event sources and then transitioning to a next state",
  "extends": {
    "$ref": "defaultstate.json"
  },
  "properties": {
    "timeDelay": {
      "type": "string",
      "description": "Amount of time (ISO 8601 format) to delay"
    }
  },
  "required": [
    "timeDelay"
  ]
}